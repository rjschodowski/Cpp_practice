#include <iostream>
using std::cin;
using std::cout;

#include <string>
using std::string;

#include <vector>
using std::vector;
using std::begin;
using std::end;

#include <algorithm>
using std::count;
using std::sort;

int main() {
  vector<int> nums = {294, 333, 801, 222};
  nums.push_back(1);
  sort(begin(nums), end(nums));
  for (auto item:nums) {
    cout << item << " ";
  }
}
